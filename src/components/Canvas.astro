<div id="sketch"></div>

<script is:inline>
	function setup() {
		const canvas = createCanvas(windowWidth * 0.8, windowHeight * 0.8)
		canvas.parent('sketch')
	}

	function draw() {
		background(16)
	}
</script>
